# React + TypeScript + Vite + Tailwind CSS

## Setup

```bash
npm install
```

## Development

```bash
npm run dev
```

Tailwind CSS v4 is enabled using `@tailwindcss/vite`.
You can write utility classes directly in `src/App.tsx`, and global Tailwind import is configured in `src/index.css`.
